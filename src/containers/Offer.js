// import { useParams } from "react-router";
import {Link} from "react-router-dom"

const Offer = (props) => {
    // const {id} = useParams();

    return (<div>
        <Link to="/">Home</Link>
    </div>
    )
}


export default Offer;